{
  "name": "drawesome-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter drawesome-studio dev",
    "build": "pnpm --filter drawesome build && pnpm --filter draw-svelte build && pnpm --filter drawesome-studio build && pnpm --filter draw-svelte-studio build",
    "build:packages": "pnpm --filter drawesome build && pnpm --filter draw-svelte build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
