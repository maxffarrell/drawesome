{
  "name": "drawesome-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter drawesome-studio dev",
    "build": "pnpm --filter drawesome build && pnpm --filter drawesome-studio build",
    "build:packages": "pnpm --filter drawesome build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
